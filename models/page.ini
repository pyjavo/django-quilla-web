[model]
name = Page
label = {{ this.title }}

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = flow
flow_blocks = heading, text-block, section-block, testimonies, sponsors, featured-post, banner, wrapper
